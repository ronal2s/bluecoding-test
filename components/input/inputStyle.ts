import { StyleSheet } from "react-native";
import colors from "../../utils/colors";

export default StyleSheet.create({
  label: {
    color: colors.grey,
  },
  input: {
    fontSize: 18,
  },
  bottomLine: {
    height: 0.2,
    width: "100%",
    marginTop: 3,
  },
});
